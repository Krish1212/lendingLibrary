// All project harcoded value should go here 
// so we could handle the langs easily
export interface Boilerplate{
    id?:string;
    langCode?:string;
    projectTitle?: string;
    projectDesc?: string;
}
